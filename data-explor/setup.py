from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Traveling the world on a mission to discover new data',
    author='hanwen jia',
    license='',
)
